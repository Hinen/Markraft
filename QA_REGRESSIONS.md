# Markdown 원문 보존 회귀 — 2026-09-21

사용자가 QA.md를 Rich로 열고 공백 하나를 입력한 뒤 저장했을 때 82줄이 변경되었다. 표의 정렬·구분선과 밑줄 이스케이프가 문서 전체에 적용된 문제다. 앞선 QA의 의미 왕복 검사와 무수정 저장 검사는 이 동작을 잡지 못했다. 앞선 PASS를 수정 후의 원문 보존까지 검증한 것으로 해석하면 안 된다.

## 수정

- 기존 Milkdown/remark의 원문 위치와 ProseMirror 변경 범위를 사용해 수정하지 않은 블록을 원문 그대로 유지한다. 문법 파서를 교체하거나 새로 구현하지 않았다.
- 일반 문자·표 셀·체크박스 수정은 가능한 경우 원문의 해당 부분만 바꾼다. 원래 상태로 undo하면 원래 Markdown 표기를 돌려준다. Raw 편집·외부 reload는 새 원문을 기준으로 삼는다.
- 구조/서식 변경이나 이스케이프·엔티티처럼 문자 위치를 직접 대응시킬 수 없는 경우에는 수정된 블록만 기존 serializer로 변환한다. 이 경우 해당 블록의 문법 표기가 정규화될 수 있다. 모든 편집의 최소 바이트 diff를 보장하지는 않는다.
- 구조 변경은 결과를 기존 파서로 다시 읽어 의미가 보존되는지 검사한다. 원문 대응/의미 보존이 불가능하면 저장을 거부하고 편집 내용을 Raw에 남긴다. 확인 후 Raw에서 수정하면 저장할 수 있다.

## 검증

- Vitest 56/56: 실제 변경 전 QA.md를 fixture로 고정하고 공백 하나의 삽입만 발생하는지 문자열 전체 비교. 문단·제목·표 셀·굵은 글씨·코드·참조 링크·체크박스, 다중 블록 변경, 구조 변경, undo, 기존 16개 fixture 편집 후 의미 보존 검사 포함.
- Playwright 15/15: QA.md 공백→저장, undo→저장, Raw 수정→Rich 공백→저장 결과를 전체 문자열로 비교. HTML 블록 옆 편집 보존, 원문 보존 오류 시 Save 거부와 Raw 복구(오류 상태 주입), 기존 12개 시나리오 포함. IPC 대역 테스트이며 네이티브 검증과 구분한다.
- Rust 테스트 11/11, TypeScript/Vite 빌드 통과. `npm run tauri build -- --bundles nsis`로 수정된 Windows x64 설치 파일 생성. 로그·SHA-256은 Git에서 제외된 `Docs/markdown-preservation/`에 기록한다.
- 사용자 종료 확인 후 수정본을 NSIS로 설치하고 **실제 WebView2/Rust IPC 회귀 4/4 PASS**를 확인했다. 설치 경로는 `C:\Users\qkrql\AppData\Local\Markraft QA\markraft.exe`다. 재현 스크립트 `Docs/markdown-preservation/native-regression.mjs`가 QA 복사본을 CRLF로 열어 파일 전체 바이트를 비교했다. 결과는 `native-results.json`, `native.log`, `native-1.png`부터 `native-4.png`에 있다. 테스트 후 임시 CDP 포트를 종료했다.
  - Rich에서 공백 하나 입력·저장: 20,995 → 20,996 bytes. 해당 공백 이외의 바이트가 모두 원본과 동일.
  - undo·저장: 원본 20,995 bytes와 완전히 동일.
  - Raw 제목 수정 후 Rich 공백 입력·저장: 의도한 두 변경만 반영된 21,000 bytes와 완전히 동일.
  - 닫기·재열기·무수정 저장: 21,000 bytes 및 수정 시간 불변.
- 원래 사용자 QA.md는 `Docs/markdown-preservation/QA.user-saved.md`에 백업했다. 복원 직전에 사용자가 추가로 수정하지 않았음을 바이트 비교한 뒤 자동 재정렬만 복구했다. 사용자가 넣은 공백 하나는 그대로 남기고 커밋에 포함하지 않는다.

Windows IME와 기존 QA의 NOT RUN 항목은 이번 수정으로 통과 처리하지 않는다.

## 탭 순서 이동과 좌우 화면 분할 — 2026-09-21

- 탭 제목 pointer drag로 순서 변경, 다른 영역으로 이동, Esc/영역 밖 drop 취소. 키보드 대안은 탭의 Alt+Shift+←/→ 및 View 메뉴의 영역 이동이다. Windows 네이티브 파일 드롭과 겹치지 않도록 HTML native drag 대신 pointer capture를 사용한다.
- Split/Merge, 영역별 선택 탭과 전역 활성 문서, 빈 영역에서 문서 열기/생성, 가운데 경계선 resize(25–75%, 더블클릭 50%)를 추가했다. 저장·편집 명령은 활성 문서로 전달한다.
- 편집기는 하나의 React 부모 아래에서 같은 key로 유지하고 CSS grid 배치만 변경한다. 탭 이동/분할 때문에 편집기를 다시 생성하지 않는다.
- Vitest 61/61 PASS: 순서/영역 변경, 문서 객체·dirty 유지, merge, 빈 영역 열기, 중복 파일의 기존 영역 선택, 닫기 후 선택 상태, 잘못된 이동 방지.
- Playwright 18/18 PASS: 실제 pointer 이동으로 탭 순서/영역 변경, 취소와 키보드 이동, 편집기 DOM 인스턴스 및 undo 유지, 활성 영역 저장 대상, resize, dirty 닫기 Cancel/Discard, 좁은 창 표시. 기존 Markdown 한 글자 원문 보존 회귀도 포함한다.
- 테스트 중 빈 영역에서 pointerdown/focus로 영역을 바꾸면 toolbar 높이 변화로 버튼 click이 취소되는 문제를 수정했다. 파일 열기 테스트 대역의 disk 목록 누락도 고쳤고, 네이티브 저장 검증과 혼동하지 않는다.
- NSIS 빌드·설치 후 실제 WebView2/Rust IPC 시나리오 **5/5 PASS**: pointer 탭 재정렬과 CodeMirror undo/저장, 좌우 활성 문서별 실제 파일 저장과 수정하지 않은 Markdown 표 원문 보존, 경계선 60% resize, 영역 간 이동/Merge와 Rich undo·원본 바이트 복원, dirty 닫기 Cancel/Discard. 편집기 DOM 인스턴스가 이동 전후 같은지도 검사했다.
- 새 설치본에서 기존 QA.md 공백·undo·Raw↔Rich·재열기 원문 보존 검증도 **4/4 PASS**. 검증은 별도 QA 복사본을 사용했다. 임시 CDP 포트는 테스트 종료 후 닫았다.
- 로컬 로그·JSON·화면 캡처·재현 스크립트·설치 파일 SHA-256은 Git 제외 경로 `Docs/tab-split-qa/`에 둔다. 탭/분할 배치의 재시작 복원과 같은 문서의 양쪽 동시 표시는 이번 범위에 포함하지 않는다.

## 탭 드래그로 분할 시작 — 2026-09-21

버튼을 먼저 눌러야 분할할 수 있던 구현을 수정했다. 탭을 편집 영역 좌우 가장자리(가로 폭의 양끝 25%)로 끌면 실제 배치될 영역의 미리보기가 나오고, 드롭 시 해당 탭으로 좌우 분할한다. 미리보기는 문서/선택 상태를 바꾸지 않는다. 비활성 탭을 끌면 기존 활성 문서는 반대쪽에 유지한다. 이미 분할된 상태에서는 다른 편집 영역의 본문 위에 놓아 이동할 수 있다.

- Vitest 64/64 PASS: 좌우 드래그 분할의 선택/dirty 유지, 문서 하나의 분할 시 중복 방지, 기존 영역 이동 및 기존 회귀.
- Playwright 21/21 PASS: Split 버튼을 누르지 않고 양쪽 edge drop, pointerup 전 미리보기·상태 불변, 비활성 탭 이동, editor DOM/undo 유지, 본문으로 영역 이동, Esc/중앙/영역 밖 드롭 취소 및 기존 회귀.
- NSIS 설치 후 실제 WebView2/Rust IPC **5/5 PASS**: 버튼 없이 왼쪽/오른쪽 edge split, 미리보기 중 단일 영역 유지, 비활성 문서의 배치, 실제 파일 저장과 undo의 원본 바이트 복원, 양방향 본문 드롭 이동과 편집기 인스턴스 유지, Esc 취소. 기존 앱은 정상 종료 후 교체했고 강제 종료하지 않았다.
- 로그·JSON·분할 미리보기/완료 화면 캡처·설치 파일 해시와 재현 스크립트는 Git 제외 경로 `Docs/drag-split-qa/`에 둔다. 테스트용 CDP 포트는 종료했다.

## 편집 작업 흐름 UX 점검 — 2026-09-21

탭/분할, 포커스/단축키, 닫기 확인, 설정창, 좁은 화면, 파일 접근 오류의 작업 흐름을 점검했다. 핵심 재현 테스트 5개를 수정 전 실행해 모두 FAIL인 것을 확인했다(`Docs/ux-audit/baseline.log`). 앱 전체의 모든 상황이 검증되었다는 뜻은 아니다.

수정한 항목:

- 마지막 탭 닫기·다른 영역으로 이동·메뉴 이동 후 빈 영역 자동 합치기. 오른쪽만 남아도 전체 화면으로 확장하고 편집기/undo/dirty를 유지한다. 문서 하나일 때 분할 버튼/드롭을 비활성화한다.
- 현재 탭을 다시 클릭하거나 드래그를 끝내면 즉시 편집 포커스를 복구한다. Save 버튼도 입력 커서를 빼앗지 않는다. Ctrl+Tab/Shift+Tab, Ctrl+PageUp/PageDown, 중간 클릭 닫기를 지원한다.
- 반대편 영역의 검색 입력으로 포커스를 옮겨도 본문이 포커스를 빼앗지 않는다. 검색창에서 Ctrl+B/I/Y를 눌러 문서에 서식/redo가 적용되는 것도 막았다. Ctrl+F를 다시 눌러도 기존 검색어를 유지한다.
- 선택한 탭을 보이도록 자동 스크롤하고, 넘치는 탭은 휠과 드래그 가장자리 연속 스크롤로 접근한다. 긴 이름은 말줄임하고 전체 경로는 툴팁으로 제공한다.
- 새 문서 이름을 중복 없이 부여한다. 닫기 확인은 경로를 표시한다. 전체 닫기는 모든 확인이 끝난 뒤 탭을 제거하므로 중간 Cancel이 앞서 선택한 Discard나 깨끗한 탭까지 닫아 버리지 않는다. 다른 문서의 저장을 기다리며 앞서 확인한 탭에 새 편집을 하면 모든 탭을 유지하고 알린다. 이미 선택한 Save의 디스크 기록은 되돌리지 않는다.
- 설정/확인/URL 대화상자는 배경을 비활성화하고 Tab 포커스를 내부에 유지한다. Esc로 취소/닫기 후 편집기로 복귀한다. 설정창 뒤에서 Ctrl+N/W/S 등의 문서 명령이 작동하지 않는다. 메뉴도 Esc/방향키를 처리하며 실행할 수 없는 명령은 비활성화한다.
- 글자 크기는 입력 도중 한 자리 숫자를 강제로 10으로 바꾸지 않고, 입력을 마치면 10–32 범위를 적용한다.
- 툴바 높이를 일정하게 유지해 분할 영역의 TXT/Rich 포커스 이동 때 본문/커서 위치가 밀리지 않게 했다. 좁은 창의 서식 도구는 가로 스크롤하고, 검색 입력도 영역 폭에 맞춘다.
- 삭제되었거나 접근할 수 없는 파일에는 동작하지 않는 Reload/Keep Mine 대신 Save As를 바로 제공한다. 새 작업을 시작하면 오래된 오류 배너를 정리한다.
- Keep Mine/Reload의 디스크 응답을 기다리는 동안 새로 입력한 내용을 보호한다. Keep Mine은 최신 메모리 내용으로 dirty를 계산하고, Reload 확인 이후 새 편집이 생기면 덮어쓰지 않고 알린다. 자동 reload도 저장 revision이 도중 바뀌면 적용하지 않는다.
- 실제 설치 앱에서 저장 직후 계속 입력하고 Ctrl+S를 다시 누르면 두 번째 요청이 무시되는 문제를 재현했다. 같은 문서의 대기 저장 요청을 합쳐 첫 작업 뒤 최신 내용을 저장하도록 수정했다. 디스크 지연을 주입해 중복 Ctrl+S가 최신 텍스트를 기록하는 회귀 검사도 추가했다.

Vitest **67/67 PASS**, Playwright **36/36 PASS**, TypeScript/Vite 빌드 PASS. 기존 드래그 분할과 Markdown 한 글자 원문 보존 회귀도 포함한다. 과거 테스트의 빈 영역 유지 기대는 이번 요구사항에 맞춰 자동 합치기 검증으로 변경했고, 단일 문서 분할 방지를 별도로 검사했다. 디스크 응답 지연 중 계속 입력하는 시나리오는 지연을 주입한 UI 회귀 검사다. 전체 닫기 중 새 편집 보호도 수정 전 FAIL을 확인한 뒤 회귀 검사에 포함했다.

NSIS 설치 후 실제 WebView2/Rust IPC로 UX 시나리오 **8/8 PASS**: 양쪽 각각 마지막 탭 닫기, 마지막 탭 이동과 undo/원문 유지, 현재 탭 클릭 및 연속 저장, 설정의 포커스/단축키/숫자 입력, 전체 닫기 Cancel과 미저장 내용 유지, 빈 화면/단일 탭의 명령 비활성화, 탭 순환·휠·연속 드래그 스크롤·중간 클릭 닫기. 별도 QA 복사본으로 기존 Markdown 공백·undo·Raw↔Rich·재열기 바이트 검증도 **4/4 PASS**다. 원래 사용자 문서는 이 검증에서 수정하지 않았다.

로그·JSON·캡처·재현 스크립트·설치 파일 SHA-256은 Git 제외 경로 `Docs/ux-audit/`에 기록한다. 검증 중 예상하지 않은 문자 입력이 포함된 실행은 실패로 보존하고, QA 복사본을 새로 열어 같은 검사로 재확인했다. 해당 문자의 발생 원인은 확정하지 않았다. Windows IME 등 기존 NOT RUN 항목은 이번 검증으로 통과 처리하지 않는다.

## Markdown 본문 너비 — 2026-09-21

Rich 본문의 최대 860px 제한과 가운데 정렬을 제거하고 각 편집 영역의 전체 너비를 사용한다. 좌우 여백은 창/분할 너비에 관계없이 24px다. 기존 64px 여백으로 좁은 분할 영역이 더 좁아지는 문제도 함께 수정했다.

TypeScript/Vite 및 NSIS 빌드, 관련 Playwright 11/11 PASS. 설치 앱의 WebView2에서 1920px/640px viewport 각각 단일·분할 상태를 측정해 4/4 PASS: 모든 영역에서 본문 너비가 스크롤바를 제외한 영역 너비보다 정확히 48px 작고 가로 넘침이 없다. 1920px 단일 화면의 실제 본문 너비는 1857px다. 캡처·측정값·검증 스크립트는 `Docs/rich-width/`에 기록했다.
